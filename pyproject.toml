[build-system]
requires = [
    "Cython",
    "cysignals",
    "setuptools>=61.2",
    "setuptools_scm[toml]>=3.4",
    "wheel"
]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "gappy/_version.py"
